"use strict";

var EventEmitter = require('events').EventEmitter;

class Encoder extends EventEmitter {

    constructor() {
        super();
    }

    createEncoder(stream, options) {
        return Promise.reject('Invalid encoder');
    }

    destroyDecoder() {

    }

}

module.exports = Encoder;